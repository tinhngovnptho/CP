/*
	Author: tinhnopro
	created: 27.02.2025 08:54:25
*/
#include <bits/stdc++.h>

#ifdef LOCAL
#include "debug.h"
#else
#define debug(...) 42
#endif // LOCAL

#define TASK "3"

using namespace std;

using i64 = long long;

const int MOD = (int) 1e9 + 7; 

int n; 

void solve() {
	cin >> n; 
	
}

int32_t main() {
	ios::sync_with_stdio(false);
	cin.tie(0);

	if (fopen(TASK".inp", "r")) {
		freopen(TASK".inp", "r", stdin);
		freopen(TASK".out", "w", stdout);
	}

	int t; cin >> t; 

	while (t--) {
		solve(); 
	}
}