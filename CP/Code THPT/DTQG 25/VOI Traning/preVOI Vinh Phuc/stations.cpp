/**
 * author: tinhnopro (tinh nop)
 * created: 2024-12-06
**/
#ifdef LOCAL
#include "debug.h"
#else
#define debug(...) 0
#endif // LOCAL

#include <bits/stdc++.h>

using namespace std;

int32_t main() {
	ios::sync_with_stdio(false);
	cin.tie(0);

	if (fopen("stations.inp", "r")) {
		freopen("stations.inp", "r", stdin);
		freopen("stations.out", "w", stdout);
	}


}

